import SwiftUI

extension Color {
    // Extracted from the Mandoline Slicer image
    static let themeBackground = Color(red: 0.99, green: 0.98, blue: 0.97) // Soft warm off-white
    static let themeContainer = Color(red: 0.93, green: 0.92, blue: 0.89) // Light beige/stone
    static let themeBorder = Color(red: 0.65, green: 0.63, blue: 0.60) // Taupe/warm gray shadows
    static let themeText = Color(red: 0.30, green: 0.28, blue: 0.25) // Dark brown/gray for text
    static let themeAccent = Color(red: 0.55, green: 0.53, blue: 0.50) // Metallic gray accent
}
